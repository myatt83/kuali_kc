diff --git a/src/main/webapp/rice-portal/scripts/easyXDM/resize_intermediate.html b/src/main/webapp/rice-portal/scripts/easyXDM/resize_intermediate.html
index 3a85216..dad3f2f 100644
--- a/src/main/webapp/rice-portal/scripts/easyXDM/resize_intermediate.html
+++ b/src/main/webapp/rice-portal/scripts/easyXDM/resize_intermediate.html
@@ -42,7 +42,7 @@
                     iframe.onload = function() {
                     	top.jQuery.unblockUI();
                         var d = iframe.contentWindow.document;
-                        var originalHeight =  jQuery(d).find('#view_div').height();
+                        var originalHeight =  jQuery(d).find('#Uif-Application').height();
                         originalHeight += heightPadding;
                         var oldHeight = originalHeight;  // to avoid flashing vertical scroll bar in Google Chrome when table labels wrap/unwrap on screen resize
 
@@ -55,7 +55,7 @@
                             timer = setInterval(function() {
                                 try {
                                     var d = iframe.contentWindow.document;
-                                    var newHeight = jQuery(d).find('#view_div').height();
+                                    var newHeight = jQuery(d).find('#Uif-Application').height();
                                     if(newHeight != originalHeight && newHeight + heightPadding != oldHeight) {
                                         // The height has changed since last we checked
                                         oldHeight = originalHeight;
